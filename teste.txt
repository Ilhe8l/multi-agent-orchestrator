const POLL_INTERVAL = 2000; const POLL_TIMEOUT = 150; // 150 × 2s = 5 min máx // Substitua o handleSend completo por este: const handleSend = useCallback(async () => { if (!input.trim() || isLoading) return; const userMessage = { id: Date.now().toString(), type: 'user', text: input, timestamp: new Date(), }; const userInput = input; setInput(''); setIsLoading(true); setConversations((prev) => prev.map((c) => { if (c.id !== currentConvId) return c; return { ...c, title: c.messages.length === 1 ? userInput.slice(0, 50) : c.title, messages: [...c.messages, userMessage], updatedAt: new Date(), }; }) ); try { // ── 1. Enfileira e recebe job_id imediatamente ── const postRes = await fetch(http://localhost:8004/api/chat, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ user_input: userInput }), }); if (!postRes.ok) throw new Error('Erro ao enfileirar mensagem'); const { job_id } = await postRes.json(); // ── 2. Polling até o resultado ficar pronto ── let data = null; for (let attempt = 0; attempt < POLL_TIMEOUT; attempt++) { await new Promise((r) => setTimeout(r, POLL_INTERVAL)); const pollRes = await fetch(http://localhost:8004/api/result/${job_id}); if (pollRes.status === 404) throw new Error('Job expirado ou não encontrado.'); const pollData = await pollRes.json(); if (pollData.status === 'done') { data = pollData; break; } if (pollData.status === 'error') throw new Error(pollData.text || 'Erro no processamento.'); // status === 'pending' → continua aguardando } if (!data) throw new Error('Timeout: o agente demorou demais para responder.'); // ── 3. Monta a mensagem (lógica idêntica à original) ── const isScalarResult = data.data && data.data.length === 1 && Object.keys(data.data[0]).length === 1; const aiMessage = { id: (Date.now() + 1).toString(), type: 'ai', text: data.type === 'fallback' ? '' : (data.text || 'Não consegui processar sua pergunta.'), final_response: data.final_response, timestamp: new Date(), sql: data.sql, table: data.data, isScalarResult, chart: !isScalarResult && data.data && data.data.length > 0 ? data.data.slice(0, 5).map((row, i) => ({ name: Object.values(row)[0] || Item ${i + 1}, value: parseFloat(Object.values(row)[1]) || 0, })) : null, isFallback: data.type === 'fallback', isChat: data.type === 'chat', fallbackScore: data.score, fallbackMessage: data.message, suggestions: data.suggestions, related_attributes: data.related_attributes, }; setConversations((prev) => prev.map((c) => { if (c.id !== currentConvId) return c; return { ...c, messages: [...c.messages, aiMessage], updatedAt: new Date() }; }) ); } catch (err) { const errorMessage = { id: (Date.now() + 1).toString(), type: 'ai', text: Erro ao processar sua pergunta: ${err.message}. Verifique se o backend está rodando., timestamp: new Date(), }; setConversations((prev) => prev.map((c) => { if (c.id !== currentConvId) return c; return { ...c, messages: [...c.messages, errorMessage], updatedAt: new Date() }; }) ); } finally { setIsLoading(false); } }, [input, isLoading, currentConvId]);